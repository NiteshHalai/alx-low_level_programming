#include"main.h"

/**
*print_line - Entry point
@c: checks input of function
*Description: 'prints line'
*/
void print_line(int n)
{
int a;
int q = '_';
{
for (a = 0; a <= n; ++a)
putchar(q);
}
putchar('\n');
}
