#include"main.h"

/**
*print_line - Entry point
@n: checks input of function
*Description: 'prints line'
*/
void print_line(int n)
{
int a;
int q = '_';
{
for (a = 0; a < n; ++a)
_putchar(q);
}

_putchar('\n');
}
