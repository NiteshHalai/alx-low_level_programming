#include"main.h"

/**
* _isdigit - function to check if
* character is lowercase
*
* @c: checks input of function
*
* Return: returns output
*/

int _isdigit(int c)
{
if (c >= 0 && c <= 9)
return (1);
return (0);
}
