#include"main.h"
/**
* _puts - prints string
*
* @str: input function argument
*
* Return: outpuy
*/
void _puts(char *str);
{
const char *to_print = *str";
unsigned long int i;
for (i = 0; i < strlen(to_print); ++i)
{
putchar(to_print[i]);
}
putchar('\n');
}
